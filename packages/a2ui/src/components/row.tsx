import { Component } from '@geajs/core'

/** A2UI `Row`. Empty by design — see the note in `column.tsx`. */
export default class Row extends Component {
  template() {
    return <div class="a2ui-row"></div>
  }
}
