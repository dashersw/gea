import { Component } from '@geajs/core'

/** A2UI `List`. Empty by design — see the note in `column.tsx`. */
export default class List extends Component {
  template() {
    return <div class="a2ui-list"></div>
  }
}
